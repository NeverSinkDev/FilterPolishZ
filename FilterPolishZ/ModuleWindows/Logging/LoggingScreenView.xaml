<UserControl x:Class="FilterPolishZ.ModuleWindows.Logging.LoggingScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:domain="clr-namespace:FilterPolishUtil.Model;assembly=FilterPolishUtil" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid>
        <DockPanel>
            <Grid>
                <DataGrid x:Name="ConfigGrid" Margin="0 8 0 0" ItemsSource="{Binding LoggingData}" CanUserSortColumns="True" CanUserAddRows="False">
                </DataGrid>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
